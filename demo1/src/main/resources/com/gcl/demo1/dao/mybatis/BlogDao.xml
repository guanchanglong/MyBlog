<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gcl.demo1.dao.mybatis.MBlogDao">
    <resultMap id="blogMap" type="com.gcl.demo1.entity.mybatis.Blog">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="firstPicture" column="first_picture"/>
        <result property="view" column="view"/>
        <result property="appreciation" column="appreciation"/>
        <result property="shareStatement" column="share_statement"/>
        <result property="commentable" column="commentable"/>
        <result property="published" column="published"/>
        <result property="recommend" column="recommend"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="typeId" column="type_id"/>
        <result property="description" column="description"/>
        <result property="userId" column="user_id"/>
        <result property="year" column="year"/>
    </resultMap>

    <select id="findAllByPublished" resultMap="blogMap">
        select * from b_blog where published = 1
    </select>

    <select id="findBlogById" resultMap="blogMap">
        select * from b_blog where id = #{blogId}
    </select>

    <update id="updateViews">
        update b_blog set views = views+1 where id = #{blogId}
    </update>

    <select id="findBlogByTypeId" resultMap="blogMap">
        select * from b_blog where type_id = #{typeId} and published = 1
    </select>

    <select id="findBlogByTag" resultMap="blogMap">
        select * from b_blog where id in (select blog_id from b_blog_tag where tag_id = #{tagId}) and published = 1
    </select>

    <select id="findBlogByContent" resultMap="blogMap">
        select * from b_blog where published = 1 and (title like #{content} or content like #{content})
    </select>

    <select id="findRecommendBlog" resultMap="blogMap">
        select * from b_blog where published = 1 and recommend = 1
    </select>

    <select id="findGroupYear" resultMap="blogMap">
        select DATE_FORMAT(create_time,'%Y') as year from b_blog group by DATE_FORMAT(create_time,'%Y') order by year desc
    </select>

    <select id="findByYear" resultMap="blogMap">
        select * from b_blog where published = 1 and DATE_FORMAT(create_time,'%Y') = #{year}
    </select>



</mapper>