#生产环境(部署到服务器上面)
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/travel?useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true
    username: travel
    password: jyMymNkE8xnAhNST
  redis:
    database: 0           # redis数据库索引（默认为0）
    host: 175.178.65.191  # redis的服务器地址
    port: 6379            # redis的服务器端口号
    password: 20001114    # redis连接密码
    jedis:
      pool:
        max-idle: 8   # 连接池中的最大空闲连接
        min-idle: 0   # 连接池中的最小空闲连接
    timeout: 300

mybatis:
  mapper-locations: classpath:mapping/*Mapper.xml
  type-aliases-package: com.xupt.demo1.entity

logging:
  level:
    root: warn
    com.gcl: info
  file:
    name: log/blog-pro.log

server:
  port: 8888
  servlet:
    session:
      timeout: 864000